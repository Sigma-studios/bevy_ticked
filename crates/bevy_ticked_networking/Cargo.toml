[package]
name = "bevy_ticked_networking"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = "0.19"
bevy_ticked = { path = "../.." }
postcard = { version = "1", features = ["alloc"] }
serde = { version = "1", features = ["derive"] }
